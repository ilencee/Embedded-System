# 2026-08-17

## 修复 12VON 电源控制模块（12VON.c / 12VON.h）

- 原始代码致命问题：所有标识符以数字 `12` 开头（`12VON_PIN`、`12VON_Init` 等），C 语言标识符不能以数字开头 → 整个文件无法编译；include guard `__12VON_H__` 同样非法。
- 修复：统一改名 `12VON_*` → `V12ON_*`（合法标识符），补齐文件头注释与 extern "C"。
- 原 `12VON.c` 用的 `RCU_EnableClock()` API 在 GD32E23x 库不存在（正确为小写 `rcu_periph_clock_enable()`），且宏名不匹配；已改用 `rcu_periph_clock_enable(V12ON_GPIO_CLK)`，并补齐 GPIO 推挽输出配置 + 默认关 + `V12ON_Set/Reset/Toggle` 实现。
- 过程中用户手动插入过一行 `RCU_PeriphClock_Enable(V12ON_GPIO_CLK);`（大写 API 同样不存在，且与正确调用重复），已删除。
- 用 MinGW gcc + 临时 stub 头文件做了 `-fsyntax-only` 验证，通过。
- **⚠️ 引脚冲突提醒：12VON 使用 PA5，而昨天新增的 DS1302(1302.c) 默认 SCLK=PA5 —— 两者同时启用会冲突，需用户确认硬件实际接线后调整其中一个的引脚宏。**
- 注意：12VON.c/h 均未加入 Keil 工程（uvprojx 无引用），需手动添加后才参与编译。
- 遗留：验证用的临时目录 `MOVA/.tmp_verify/`（含 stub 头文件）仍在磁盘上，用户拒绝了删除命令，待用户自行清理。
- **2026-08-17 补充：用户确认 12VON 实际引脚为 PB8**（`GPIO_PIN_8` / `GPIOB` / `RCU_GPIOB`），已更新 `12VON.h` 宏 `V12ON_PIN/V12ON_GPIO/V12ON_GPIO_CLK` 及 `12VON.c` 注释；与 DS1302 的 PA5 冲突问题随之解除（PB8 不与任何现有外设冲突）。
- **2026-08-17：工程文件夹 MOVA → TEST（现位于 `Desktop\Embedded\TEST`）**。修复了两处残留旧绝对路径：
  - `TEST/Project/MOVA.uvoptx:139` 断点 `<Filename>`：`C:\Users\86249\Desktop\MOVA\Project\main.c` → `C:\Users\86249\Desktop\Embedded\TEST\Project\main.c`
  - `TEST/Project/MOVA.uvguix.86249:9` `<LastAddFilePath>`：`Desktop\MOVA\User` → `Desktop\Embedded\TEST\User`
  - 说明：`MOVA.uvprojx` 全部为相对路径（`..\User` 等），仅 TargetName/OutputName 含 MOVA（工程名，与文件夹名无关，勿改）；若 Keil 仍异常需执行 Rebuild 清缓存（Objects 下 .o/.d/.dep 为相对路径缓存）。
- **2026-08-17：同步修复 `.vscode` 配置**：
  - `c_cpp_properties.json`：7 处 `${workspaceFolder}/MOVA` → `${workspaceFolder}/TEST`；移除无效路径 `TEST/Project/Device/Include`（目录不存在）；移除未安装的 DFP pack 路径 `C:/Users/86249/AppData/Local/Arm/Packs/GigaDevice/GD32E23x_DFP/2.5.0/Device/Include`（实测 `AppData/Local/Arm/Packs` 仅有 `.Web` 元数据，pack 未解压安装；器件头文件由 `TEST/Libraries/CMSIS/GD/GD32E23x/Include` 完整提供）。
  - `tasks.json`：3 处 `${workspaceFolder}\\MOVA\\Project` → `${workspaceFolder}\\TEST\\Project`（任务 cwd）。
  - 保持不变的：`TEST/Project/RTE/_MOVA`（RTE 目录名=工程名）、`MOVA.uvprojx` 文件名、任务名/label 中的 MOVA。
  - lint 已清零。
- **2026-08-17：同步修复 `.vscode/c_cpp_properties.json`**：全部 `${workspaceFolder}/MOVA/...` → `${workspaceFolder}/TEST/...`（共 9 处），并删除不存在的 `${workspaceFolder}/TEST/Project/Device/Include`（器件头文件实际来自 DFP 包路径）。最终 includePath 与 uvprojx 相对路径（`..\User;..\User\core;..\User\drivers;..\User\app;..\Libraries\CMSIS\GD\GD32E23x\Include;..\Libraries\GD32E23x_standard_peripheral\Include;..\Libraries\CMSIS;`）一一对应。注意：编辑期间文件被外部并发修改，最终状态以磁盘为准。
- **2026-08-17：新增 Triac（可控硅）驱动模块** `TEST\User\drivers\triac.c/h`：
  - 4 路通道：TRIAC_CH1~4 = PB1/PB2/PB10/PB11（均 GPIOB，RCU_GPIOB），通道号 1~4，非法通道号静默忽略。
  - API：`Triac_Init()`（推挽输出+默认全关）、`Triac_SwitchOn/Off(ch)`、`Triac_Toggle(ch)`、`Triac_SwitchOnAll/OffAll()`。
  - 驱动风格沿用 fan/pump（ilencee 头注释、V1.0.0、高电平有效）；注释提醒 PB2 为 BOOT1 引脚、触发脉冲方案需配合 zero.c 过零检测。
  - **已将 `triac.c` 加入 `MOVA.uvprojx` 的 USER 组**（uart.c 之后），用 ARMCLANG `-fsyntax-only` 验证通过（exitCode=0）。
  - 注意：`compile_commands.json` 仍是重命名前的旧缓存（路径指向 MOVA 且缺 drivers 子目录），未修改；如需更新可后续重建。
- **2026-08-17 修订：Triac 改为按负载命名**（用户指出四路负载为 HEAT/PTC/UVA/UVB）：
  - `triac_ch_t` 枚举：`TRIAC_HEAT=PB1`、`TRIAC_PTC=PB2`、`TRIAC_UVA=PB10`、`TRIAC_UVB=PB11`，`TRIAC_CH_NUM` 置于末尾；不再用泛化 CH1~4 宏。
  - API 参数改为 `triac_ch_t`，新增便捷函数 `Triac_HeatOn/Off`、`Triac_PtcOn/Off`、`Triac_UvaOn/Off`、`Triac_UvbOn/Off`。
  - 版本号升为 V1.1.0，ARMCLANG `-fsyntax-only` 验证通过（exitCode=0）。
- **2026-08-17 再修订：负载通道改为独立宏**（用户要求：负载名称全部大写 + 不用枚举）：
  - 删除 `triac_ch_t` 枚举，改为 `#define TRIAC_HEAT 0U / TRIAC_PTC 1U / TRIAC_UVA 2U / TRIAC_UVB 3U / TRIAC_CH_NUM 4U`（大写宏）。
  - API 参数恢复为 `uint8_t ch`，便捷函数不变；版本号 V1.2.0，ARMCLANG `-fsyntax-only` 验证通过（exitCode=0）。
  - **用户代码风格偏好：不喜欢枚举，偏好独立大写宏 + 便捷函数**，已记入长期记忆。
- **2026-08-17 再修订：每个负载直接定义 IO 口宏**（用户要求"直接定义IO口，不要写看不懂"）：
  - `triac.h` 中每个负载定义 `TRIAC_xxx_PORT` + `TRIAC_xxx_PIN` 独立宏（如 `TRIAC_HEAT_PORT=GPIOB` / `TRIAC_HEAT_PIN=GPIO_PIN_1`），一眼可见引脚。
  - `triac.c` 用 `triac_load_t{port,pin}` 结构体表 `s_triac_loads[4]` 表驱动，初始化/开关全部走表；通道号宏保留。
  - 版本号 V1.3.0，ARMCLANG `-fsyntax-only` 验证通过（exitCode=0）。
- **2026-08-17 再修订：去掉结构体，switch 直连**（用户问"为什么老是热衷于结构体"）：
  - 删除 `triac_load_t` 结构体、`s_triac_loads` 表、`triac_ch_valid()`；`Triac_Init()` 改为逐引脚 4 组直接配置；`Triac_SwitchOn/Off/Toggle` 用 `switch(ch)` 每个 case 一行直接操作对应 PORT/PIN 宏；OnAll/OffAll 直接 4 行。
  - 版本号 V1.4.0，ARMCLANG `-fsyntax-only` 验证通过（exitCode=0）。
- **2026-08-17 再修订：Triac 改为光耦驱动、低电平有效**（用户明确硬件特性）：
  - 导通=输出低电平（`gpio_bit_reset`），关断=输出高电平（`gpio_bit_set`）；`Triac_Init()` 上电默认关断=全部拉高。
  - `Triac_SwitchOn/Off/Toggle/OnAll/OffAll` 及 8 个便捷函数全部按低电平语义实现；h/c 头注释注明"光耦驱动，低电平有效"。
  - 版本号 V1.5.0，ARMCLANG `-fsyntax-only` 验证通过（exitCode=0），无 lint 错误。
  - 注意：h 文件曾在外部被回退到 V1.3.0，重读后已统一为 V1.5.0。
